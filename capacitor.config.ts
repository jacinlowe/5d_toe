import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lowecaintech.seventactoe',
  appName: '7_Tac_toe',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
